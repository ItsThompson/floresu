# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.eggs/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
coverage.xml
htmlcov/

# Node / frontend
node_modules/
dist/
*.local
.vite/
frontend/coverage/

# Environment and secrets
.env
.env.*
!.env.example
# .env.prod is the COMMITTED, non-secret half of the production config (see the
# file header). Real secrets live only in GitHub Actions, never here.
!.env.prod
# Private keys and credential dumps. No negations here: a file of either shape
# is never committed.
*.pem
*token*.json

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
